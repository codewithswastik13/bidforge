# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Settings & validation ---
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Database ---
SQLAlchemy[asyncio]==2.0.35
# asyncmy==0.2.9  # MySQL async driver - not needed for the SQLite local mode (no MSVC build required)
alembic==1.13.2

# --- Redis / realtime ---
redis==5.0.8

# --- Auth ---
bcrypt==4.2.0
PyJWT==2.9.0
python-multipart==0.0.9

# --- Observability ---
prometheus-client==0.20.0

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
aiosqlite==0.20.0
